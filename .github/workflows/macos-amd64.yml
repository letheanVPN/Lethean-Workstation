name: macOS Intel
permissions:
  contents: write

# Build for everything on "main" & "v*.*.*"
on:
  push:
    branches:
      - 'main'
      - 'v*'
    tags:
      - 'v*'
    paths-ignore:
      - '**.md'
  pull_request:
    branches:
      - 'main'
      - 'v*'
    paths-ignore:
      - '**.md'

jobs:
  macos:
    runs-on: macos-latest
    steps:
      # Checkout code
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      # Install / Setup Nodejs v16
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      # Install GoLang
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.17
      # Setup node module caching
      - name: Cache node modules
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          # npm cache files are stored in `~/.npm` on Linux/macOS
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      # Set up GoLang cache
      - name: Go Cache
        uses: actions/cache@v2
        with:
          path: |
            ~/Library/Caches/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
      # Helper NPM module install
      - run: npm install --legacy-peer-deps
      # Install desktop-angular-app dependencies
      - run: cd frontend && npm install --legacy-peer-deps && npm run build
      # runs deno installer
      - name: Install Deno
        run: curl -fsSL https://deno.land/x/install/install.sh | sh && echo "$HOME/.deno/bin" >> $GITHUB_PATH
      # runs: wails build creating a packaged exe
      - name: Build
        run: wails build --platform darwin/amd54
      - run: chmod +x build/bin/lethean.app/Contents/MacOS/lethean
      - name: 'Tar files'
        run: cd build/bin/ && tar -cvf ../../macos-amd64-lethean-desktop.tar .
      # uploads the job file, makes no release
      - uses: actions/upload-artifact@v2
        with:
          name: macOS Intel Desktop
          path: macos-amd64-lethean-desktop.tar
      # if this is a tag build, upload
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: macos-amd64-lethean-desktop.tar
      - name: Release Branch
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/heads/main')
        with:
          tag_name: canary
          prerelease: true
          files: macos-amd64-lethean-desktop.tar

